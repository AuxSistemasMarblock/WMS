services:
  wms-backend:
    build: ./backend
    container_name: wms-backend
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=3001
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - JWT_SECRET=${JWT_SECRET}
      - NETSUITE_ACCOUNT_ID=${NETSUITE_ACCOUNT_ID}
      - NETSUITE_REALM=${NETSUITE_REALM}
      - NETSUITE_ENVIRONMENT=${NETSUITE_ENVIRONMENT}
      - NETSUITE_API_VERSION=${NETSUITE_API_VERSION}
      - NETSUITE_CLIENT_ID=${NETSUITE_CLIENT_ID}
      - NETSUITE_CLIENT_SECRET=${NETSUITE_CLIENT_SECRET}
      - NETSUITE_TOKEN_ID=${NETSUITE_TOKEN_ID}
      - NETSUITE_TOKEN_SECRET=${NETSUITE_TOKEN_SECRET}
      - NETSUITE_RESTLET_URL=${NETSUITE_RESTLET_URL}
      - NETSUITE_RESTLET_SCRIPT_ID=${NETSUITE_RESTLET_SCRIPT_ID}
      - NETSUITE_RESTLET_DEPLOY_ID=${NETSUITE_RESTLET_DEPLOY_ID}
      - NETSUITE_FOLDER_MEX_AUXALMACEN=${NETSUITE_FOLDER_MEX_AUXALMACEN}
      - NETSUITE_FOLDER_MEX_CLIENTE=${NETSUITE_FOLDER_MEX_CLIENTE}
      - NETSUITE_FOLDER_MEX_JEFE=${NETSUITE_FOLDER_MEX_JEFE}
      - NETSUITE_FOLDER_MEX_GERENTE=${NETSUITE_FOLDER_MEX_GERENTE}
      - NETSUITE_FOLDER_GDL_AUXALMACEN=${NETSUITE_FOLDER_GDL_AUXALMACEN}
      - NETSUITE_FOLDER_GDL_CLIENTE=${NETSUITE_FOLDER_GDL_CLIENTE}
      - NETSUITE_FOLDER_GDL_JEFE=${NETSUITE_FOLDER_GDL_JEFE}
      - NETSUITE_FOLDER_GDL_GERENTE=${NETSUITE_FOLDER_GDL_GERENTE}
      - NETSUITE_FOLDER_MTY_AUXALMACEN=${NETSUITE_FOLDER_MTY_AUXALMACEN}
      - NETSUITE_FOLDER_MTY_CLIENTE=${NETSUITE_FOLDER_MTY_CLIENTE}
      - NETSUITE_FOLDER_MTY_JEFE=${NETSUITE_FOLDER_MTY_JEFE}
      - NETSUITE_FOLDER_MTY_GERENTE=${NETSUITE_FOLDER_MTY_GERENTE}
      - NETSUITE_FOLDER_TEMPORAL_AUXALMACEN=${NETSUITE_FOLDER_TEMPORAL_AUXALMACEN}
      - NETSUITE_FOLDER_TEMPORAL_CLIENTE=${NETSUITE_FOLDER_TEMPORAL_CLIENTE}
      - NETSUITE_FOLDER_TEMPORAL_JEFE=${NETSUITE_FOLDER_TEMPORAL_JEFE}
      - NETSUITE_FOLDER_TEMPORAL_GERENTE=${NETSUITE_FOLDER_TEMPORAL_GERENTE}
      - NETSUITE_FOLDER_PROYECTOS_AUXALMACEN=${NETSUITE_FOLDER_PROYECTOS_AUXALMACEN}
      - NETSUITE_FOLDER_PROYECTOS_CLIENTE=${NETSUITE_FOLDER_PROYECTOS_CLIENTE}
      - NETSUITE_FOLDER_PROYECTOS_JEFE=${NETSUITE_FOLDER_PROYECTOS_JEFE}
      - NETSUITE_FOLDER_PROYECTOS_GERENTE=${NETSUITE_FOLDER_PROYECTOS_GERENTE}
      - NETSUITE_FILECABINET_PATH_PREFIX=${NETSUITE_FILECABINET_PATH_PREFIX}
      - NETSUITE_FILECABINET_SIGNATURE_FOLDER_PATTERN=${NETSUITE_FILECABINET_SIGNATURE_FOLDER_PATTERN}
      - NETSUITE_FILECABINET_FILE_PATTERN=${NETSUITE_FILECABINET_FILE_PATTERN}
      - NETSUITE_SEARCH_RESTLET_URL=${NETSUITE_SEARCH_RESTLET_URL}
      - NETSUITE_SEARCH_RESTLET_SCRIPT_ID=${NETSUITE_SEARCH_RESTLET_SCRIPT_ID}
      - NETSUITE_SEARCH_RESTLET_DEPLOY_ID=${NETSUITE_SEARCH_RESTLET_DEPLOY_ID}
      - NETSUITE_SEARCH_ID=${NETSUITE_SEARCH_ID}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
      - LOG_LEVEL=${LOG_LEVEL}
    ports:
      - "3001:3001"

  wms-frontend:
    build: .
    container_name: wms-frontend
    restart: always
    ports:
      - "8080:80"
    depends_on:
      - wms-backend
