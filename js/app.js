/**
 * WMS · Aplicación Principal
 * Escaneo de PLACAS únicamente
 */

let scanMode = 'placa';

/**
 * Inicializa la aplicación
 */
function initApp() {
    renderEmpty();
}

// Inicializa la app cuando el DOM está listo
document.addEventListener('DOMContentLoaded', initApp);

// Inicializa la app cuando el DOM está listo
document.addEventListener('DOMContentLoaded', initApp);
